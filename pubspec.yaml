name: cafe_gourmet
description: Projeto Café Gourmet.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.1.1
  firebase_auth: ^4.1.1
  firebase_database: ^10.0.4
  firebase_storage: ^11.0.4
  flutter_native_splash: ^2.2.14
  get: ^4.6.5
  flutter_spinkit: ^5.1.0
  font_awesome_flutter: ^10.2.1
  google_sign_in: ^5.4.2
  carousel_slider: ^4.1.1
  flutter_rating_stars: ^1.0.3+4
  intl: ^0.17.0
  cached_network_image: ^3.2.2
  flutter_slidable: ^2.0.0
  badges: ^2.0.3
  flutter_phoenix: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.10.0
  change_app_package_name: ^1.1.0

flutter_native_splash:
  color: "#086971"
  image: assets/icone_branco.png
  branding: assets/branding.png
  android_12:
    image: assets/icone_branco.png
    icon_background_color: "#086971"
    branding: assets/branding.png
  web: false
  fullscreen: true

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path_ios: "assets/icon_adaptive.png"
  image_path_android: "assets/icon_adaptive.png"
  adaptive_icon_foreground: "assets/icon_adaptive.png"
  adaptive_icon_background: "#086971"
  remove_alpha_ios: true
  min_sdk_android: 21
  web:
    generate: false
  windows:
    generate: false

flutter:
  uses-material-design: true

  assets:
     - assets/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
